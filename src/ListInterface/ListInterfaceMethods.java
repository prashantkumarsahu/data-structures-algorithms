package src.ListInterface;

public class ListInterfaceMethods {

    public static void main(String[] args) {

         /*
            Methods from List interface

            int size = list.size(); // From Collection Interface
            boolean empty = list.isEmpty(); // From Collection Interface
            boolean contains = list.contains(10); // From Collection Interface
            list.add(10); // From Collection Interface

            list.add(1, 20);
            E element = list.get(0);
            list.set(0, 30);

            list.remove(0); // From Collection Interface
            list.remove(Integer.valueOf(10));
            list.clear();

            int index = list.indexOf(10);
            int lastIndex = list.lastIndexOf(10);

            List<E> sublist = list.subList(1, 3);

            list.addAll(otherList);
            list.addAll(1, otherList);
            list.removeAll(otherList); // remove only elements present in otherlist, preserves others
            list.retainAll(otherList); // retain all elements present in otherlist, remove others

            Iterator<E> iterator = list.iterator(); // From Collection Interface
            ListIterator<E> listIterator = list.listIterator();
            ListIterator<E> listIterator = list.listIterator(2);

            Object[] array = list.toArray();
            Integer[] array = list.toArray(new Integer[0]);


         */
    }
}
