package src.ListInterface;

public class ArrayListMethods {

    public static void main(String[] args) {

        // ArrayList class implements the List interface


        /*

        list.sort(Comparator.naturalOrder());

        ArrayList<Integer> list = new ArrayList<>();
        list.ensureCapacity(100);  // Ensure capacity for 100 elements

        ArrayList<Integer> list = new ArrayList<>(100);
        list.add(1);
        list.add(2);
        list.trimToSize();  // Trims capacity to match the current size (2 elements)

        ArrayList<Integer> list = new ArrayList<>(Arrays.asList(1, 2, 3));
        list.forEach(System.out::println);

        ArrayList<Integer> list = new ArrayList<>(Arrays.asList(1, 2, 3, 4, 5));
        list.removeIf(n -> n % 2 == 0);  // Removes all even numbers

        ArrayList<Integer> list = new ArrayList<>(Arrays.asList(1, 2, 3));
        list.replaceAll(n -> n * 2);  // Multiplies each element by 2

        ArrayList<Integer> list = new ArrayList<>(Arrays.asList(3, 1, 2));
        list.sort(Integer::compareTo);  // Sorts the list in natural order


         */
    }
}
